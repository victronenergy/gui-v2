#ifndef _VELIB_BASE_BASE_H
#define _VELIB_BASE_BASE_H

/**
 * @ingroup VELIB
 * @defgroup VELIB_BASE Base
 * @brief
 *	Definition of types and platform / compiler related things.
 * @details
 *	When the platform is recognized un8 / un16 etc will be set to the correct
 *	type and support for packing is added when possible. The binary definition 
 *	b00001101 etc are also included in base.

 */

#include <velib/base/types.h>
#include <velib/base/compiler.h>
#include <velib/base/bindef.h>

#endif
